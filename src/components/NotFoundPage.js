import React from 'react';
import {Link} from 'react-router-dom';

const NotFoundPage = ()=>(
    <div>
    404
    THis if from not found component <Link to='/'> Home page </Link>
    </div>
);

export default NotFoundPage;