import React from 'react';
import {Link} from 'react-router-dom';

const NotFoundPage = ()=>(
    <div>
    404
    This is from not found component <Link to='/'> Home page </Link>
    </div>
);

export default NotFoundPage;